package algo.sorting;

public class SelectionBasedOnQuickSort {
	
	public static <T extends Comparable<T>> int findMin(T[] arr, int low, int high) {
		int mid = 0;
		mid = QuickSort.partition(arr, low, high);
        if()		
		
	}

}
